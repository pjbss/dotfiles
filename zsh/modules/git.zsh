#!/bin/sh
# ported from oh-my-zsh git plugin; audited against ~/.zsh_history (7,565
# commands reviewed) against all ~190 aliases and 17 functions in the
# plugin — none were used. This user types `git` directly rather than
# using plugin shorthand, so nothing was ported. File exists (and is
# sourced by the module loader) to document that the audit happened and
# concluded "nothing to port," per the PRD's usage-based porting
# methodology, rather than silently skipping the module.
